{% trans_default_domain 'EsCoreBundle' %} 
{% extends ('@EsCore/layout.html.twig') %}

{% block body %}
<div class="login-box">
  <div class="login-logo">
    <b>Es</b>Core
  </div>
  <!-- /.login-logo -->
  <div class="card">
    <div class="card-body login-card-body">
      <p class="login-box-msg">{{'describe_forget_password_request_confirm'|trans|raw}}</p>

      <p class="mt-3 mb-1">
        <a href="{{path('es_core_login')}}">{{'return'|trans}}</a>
      </p>

    </div>
    <!-- /.login-card-body -->
  </div>
</div>

{% endblock %}